#**
 *               In the name of Allah
 * This file is part of The Zekr Project. Use is subject to
 * license terms.
 *
 * @version 1
 *#

#set ($INLINE = "INLINE")
#set ($BLOCK = "BLOCK")

#set ($MINOR = 0)
#set ($MAJOR = 1)

#set ($RPAREN = "&#64831;")
#set ($LPAREN = "&#64830;")

#foreach ($AYA in $AYA_LIST)
	#set($AYA_NUM = $velocityCount)
	#foreach ($JOZ in $JOZ_LIST)
		#if ($JOZ.AyaNumber == $AYA_NUM)
			<nobr><span style="color: red;" title="$JOZ">[$JOZ.Index]</span></nobr>
		#end
	#end
	<a name="#${AYA_NUM}">
	#if ($LAYOUT == $BLOCK)
		<div class="aya" id="${SOORA}_${AYA_NUM}">
	#elseif ($LAYOUT == $INLINE)
		<span class="aya" id="${SOORA}_${AYA_NUM}">
	#end
			$AYA
			<span title="${SOORA_NAME} - ${AYA_NUM}">
				<nobr><span id="sign_${SOORA}_${AYA_NUM}" class="ayaSign">$RPAREN$AYA_NUM$LPAREN</span></nobr>
			</span>
			#foreach ($SAJDA in $SAJDA_LIST)
				#if ($SAJDA.AyaNumber == $AYA_NUM)
					#if ($SAJDA.Type == $MINOR)
						<span id="minor" class="sajda" title="$SAJDA">&nbsp;*&nbsp;</span>
					#elseif ($SAJDA.Type == $MAJOR)
						<span id="major" class="sajda" title="$SAJDA">&nbsp;**&nbsp;</span>
					#end
				#end
			#end
	#if ($LAYOUT == $BLOCK)
		</div>
	#elseif ($LAYOUT == $INLINE)
		</span>
	#end
	</a>
#end